# New note diagram

```mermaid
sequenceDiagram
    participant user
    participant browser
    participant server

    user->>browser: Enter note and click Save
    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note
    activate server
    server-->>browser: 201 created/Confirmation message
    deactivate server

    Note right of browser: The browser updates the UI to display the new note